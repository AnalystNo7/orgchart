# Журнал решений (DECISIONS.md)
<!-- Новые записи сверху. Формат: дата, суть, причина. 3–5 строк, не эссе. -->

## 2026-08-19 — LLM-пресеты в БД вместо env, ключи plaintext+маска
- Решение: LlmSetting в БД, активный пресет для всех LLM-вызовов, env — fallback;
  ключи открытым текстом (наружу только маска); openai_compatible через
  createOpenAI().chat() без новой зависимости; «одна активная» — транзакцией
- Почему: смена модели/провайдера без редеплоя (Gonka ~бесплатен); частичный
  unique-индекс Prisma не выражает (drift); .chat() покрывает Chat Completions
- Затронуто: prisma, src/lib/ai/provider.ts, orchestrator, gaps, api/admin/llm*, admin/llm

## 2026-08-19 — Harness перенесён в dev-mvp3, карта привязана к ветке
- Решение: harness-структура живёт в dev-mvp3; PROJECT.md переписан по факту
  кода этой ветки, а не перенесён от MVP
- Почему: карта читается перед каждой задачей — карта от другой ветки врёт
  (в MVP «нет миграций», в dev-mvp3 их 5)
- Затронуто: CLAUDE.md, harness/*, tasks/

## ГГГГ-ММ-ДД — <короткая суть решения>
- Решение: <что решили>
- Почему: <причина — обязательна; без неё запись бесполезна>
- Затронуто: <файлы / модули>
